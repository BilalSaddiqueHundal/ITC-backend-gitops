apiVersion: v1
kind: Service
metadata:
  name: connectionservice
  labels:
    app: connectionservice
spec:
  selector:
    app: connectionservice
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP

