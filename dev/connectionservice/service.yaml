apiVersion: v1
kind: Service
metadata:
  name: connectionservice
  namespace: backend-dev
spec:
  selector:
    app: connectionservice
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
