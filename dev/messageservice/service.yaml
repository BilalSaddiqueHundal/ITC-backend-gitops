apiVersion: v1
kind: Service
metadata:
  name: messageservice
  namespace: backend-dev
spec:
  selector:
    app: messageservice
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
